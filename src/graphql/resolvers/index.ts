import { resolvers as AnimeHomeResolver } from './animeFenixHome.resolvers'
import { resolvers as verAnimeResolver } from './watchAnime.resolvers'
import { resolvers as infoAnimeResolver } from './infoAnime.resolvers'
import { resolvers as searchAnimeResolver } from './searchAnime.resolvers'
import { resolvers as downloaderAnimeResolver } from './downloaderAnome.resolvers'

export {
  AnimeHomeResolver,
  verAnimeResolver,
  infoAnimeResolver,
  searchAnimeResolver,
  downloaderAnimeResolver
}
